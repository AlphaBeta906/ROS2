---
title: Proclamations
layout: layouts/base.njk
eleventyExcludeFromCollections: true
---
On this page, you will see the proclamations for the Republic of Solemania.

<h2>List of Proclamations</h2>

{% set procs = collections.proclamations %}
<ul class="list-disc">
	{% for post in procs %}
	<li>
		<p>
			<strong>
				<a href="{{ post.url | url }}">{{ post.data.title }}</a>
			</strong>
			-
			{% if not post.data.unfinished %}
			<time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
			{% else %}
			Work In Progress
			{% endif %}
			-
			<span>{{ post.data.description }}</span>
		</p>
	</li>
	{% endfor %}
</ul>